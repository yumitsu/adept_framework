
Adept.Core.namespace('Adept.Validator');

Adept.Validator.Requared  = Class.create(Adept.Validator.Abstract,
{
    check: function()
    {
        var value = this.getComponent().value;
        if(value.strip() != ''){
            return true;
        }
        return false;
    }
    
});

